# Backend services

Each service is self-contained in its own directory with its own container:

- [`fire_spread/`](fire_spread/README.md) — ELMFIRE-based fire-spread forecast API (`GET /fire/arrival-grid`).
