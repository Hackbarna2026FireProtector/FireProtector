fastapi==0.141.1
uvicorn[standard]==0.53.0
psycopg[binary,pool]==3.3.6
pydantic-settings==2.15.0

# fire_spread: the Deepfire client and the arrival-grid rasteriser.
# shapely is pinned to the same version scripts/requirements.txt uses.
httpx==0.28.1
shapely==2.1.2
numpy==2.5.3
python-dotenv==1.2.3
